/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel follows symlinks at build time — this ensures
  // content/ and info/ symlinks to the repo root resolve.
};

export default nextConfig;